# Copy to .env and fill in real values. Never commit .env.

POSTGRES_CONNECTION=Host=localhost;Port=5433;Database=econadvisor;Username=econadvisor;Password=changeme
POSTGRES_USER=econadvisor
POSTGRES_PASSWORD=changeme
POSTGRES_DB=econadvisor

# Dify — get from http://localhost → your workflow → API Access
DIFY_API_BASE=http://localhost/v1
DIFY_WORKFLOW_KEY=app-xxxxxxxxxxxxxxxxxxxx

# External data APIs (public — no auth required)
RIKSBANK_API_BASE=https://api.riksbank.se/swea/v1
SCB_API_BASE=https://api.scb.se/OV0104/v1/doris/sv/ssd
WORLDBANK_API_BASE=https://api.worldbank.org/v2

# ELK (Phase 5)
ELASTICSEARCH_URL=http://localhost:9200
